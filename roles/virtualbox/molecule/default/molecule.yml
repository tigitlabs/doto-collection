---
dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml
driver:
  name: vagrant
  provider:
    name: virtualbox

platforms:
  - name: vagrant-ubuntu2204
    box: ubuntu/focal64

provisioner:
  name: ansible
  log: true
